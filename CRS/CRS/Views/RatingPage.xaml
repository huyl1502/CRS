<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CRS.Views"
             x:Class="CRS.Views.RatingPage"
             xmlns:material3="clr-namespace:Plugin.MaterialDesignControls.Material3;assembly=Plugin.MaterialDesignControls"
             RootViewModel="{Binding}"
             ControlTemplate="{StaticResource BaseTemplate}"
             xmlns:vm="clr-namespace:CRS.ViewModels"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:RatingViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#4A6572</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid BackgroundColor="#f7f7f7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout BackgroundColor="{StaticResource Accent}" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView Padding="0,40,0,40" VerticalOptions="FillAndExpand">
                    <Image Source="_logo.png" VerticalOptions="Center" HeightRequest="64" />
                </ContentView>
            </StackLayout>
        </StackLayout>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
                <material3:MaterialCard
                    BackgroundColor="White"
                    Type="Filled">
                    <material3:MaterialLabel
                        Type="TitleLarge"
                        Text="Xin quý khách vui lòng đánh giá chất lượng phục vụ" />
                </material3:MaterialCard>
                <material3:MaterialCard
                    BackgroundColor="White"
                    Type="Filled"
                    CornerRadius="10">
                    <material3:MaterialLabel
                        Type="LabelMedium"
                        Text="Giao dịch viên" />
                </material3:MaterialCard>
                <material3:MaterialCard
                    BackgroundColor="White"
                    Type="Filled"
                    CornerRadius="10">
                    <StackLayout>
                        <material3:MaterialLabel
                        Type="LabelMedium"
                        Text="Đánh giá" />
                        <material3:MaterialRating
                            ItemsByRow="10"
                            ItemSize="4"
                            SelectedIcon="starSelectedIcon.png" 
                            UnSelectedIcon="starUnselectedIcon.png" />
                    </StackLayout>
                </material3:MaterialCard>
                <material3:MaterialButton
                    Text="Submit" 
                    Command="{Binding OpenWebCommand}"
                    BackgroundColor="{StaticResource Primary}" />
            </StackLayout>
        </ScrollView>
    </Grid>

</local:BasePage>

